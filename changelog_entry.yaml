- bump: patch
  changes:
    added:
      - Updates `.gitignore`
      - Adds `Makefile`
      - Updates `README.md`
      - Adds `CHANGELOG.md`, `changelog.yaml`, and `changelog_entry.yaml`
      - Adds `environment.yml`, `setup.py`, and `CONTRIBUTING.md`
      - Updates stuff in the `/docs/` folder
      - Adds `/code/`, `/data/`, and `/tests/` folders
      - Adds `.github` folder for GitHub Actions
      - Runs `make format` and `make documentation` with no issues.
